version: 0.1
frontend:
  phases:
    build:
      commands:
        - npm ci
        - npm run lint
  artifacts:
    baseDirectory: /docs
    files:
      - '**/*'
  cache:
    paths:
      - '$HOME/.npm'
